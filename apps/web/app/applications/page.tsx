'use client';
import React from 'react';
export default function Applications() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-4">Application Tracker</h1>
      <div className="card">Kanban / table of Saved → Applied → Recruiter screen → ... → Offer / Rejected. Full CRUD + JD snapshot + resume version used + follow-up suggestions in complete build (PR-09+). Current: use /jobs to analyze and manually track.</div>
    </div>
  );
}
