'use client';
export default function Onboarding() {
  return (
    <div className="max-w-xl">
      <h1 className="text-2xl mb-2">Welcome to ReboundIQ</h1>
      <p>Enter layoff details, visa status (optional, encrypted), runway, targets. Generates 30/60/90 plan + risk dashboard.</p>
      <div className="card mt-4">Full intake form + planner in complete implementation. For this slice start with Resume or JD Match from nav.</div>
    </div>
  );
}
